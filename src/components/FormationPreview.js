import React from 'react';

const FormationPreview = ({ starters }) => {
  // Your code for rendering the formation preview based on starters
  return (
    <div>
      <h2>Formation Preview</h2>
      {/* Your rendering logic here */}
    </div>
  );
};

export default FormationPreview;
